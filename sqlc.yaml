# version: "2"
# sql:
#   - engine: "postgresql"
#     queries: "sql/queries/"
#     schema: "sql/migrations/"
#     gen:
#       go:
#         package: "sqlc"
#         out: "internal/pkg/database/sqlc"
#         sql_package: "pgx/v5"
#         emit_json_tags: true

version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/queries/"
    schema: "sql/migrations/"
    gen:
      go:
        package: "sqlc"
        out: "internal/pkg/database/sqlc"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: false
        emit_exact_table_names: false
        emit_empty_slices: true
        overrides:
          - db_type: "pg_catalog.timestamp"
            go_type: "time.Time"
          - db_type: "pg_catalog.timestamptz"
            go_type: "time.Time"
